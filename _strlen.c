#include "shell.h"
/**
 * _strlen - check the code for Holberton School students.
 * @s: Pointer s.
 * Return: Always 0.
 */
int _strlen(char *s)
{
	int v;

	for (v = 0; s[v] != '\0'; v++)
	{
		;
	}
	return (v);
}
